
# LONG-TERM MEMORY

Use the following JSON only when relevant. Do not repeat it verbatim to the user.
Memory reflects **user-stated** information only (not assistant recommendations). Fields are free-form; names are suggestions, not a fixed schema. Keys prefixed with `_` are metadata.

```json
{memory_json}
```
